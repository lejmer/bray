trusted module target_control_invalid;

trusted func invalid_assembly() -> i32
    uses(device_memory, intrinsic, raw_memory, unchecked_alias, unchecked_init)
{
    return trusted core.target.assembly<i32, i32>(
        template = "",
        constraints = "={x0},r",
        clobbers = "",
        features = "",
        options = 0,
        input = 1,
    );
}
