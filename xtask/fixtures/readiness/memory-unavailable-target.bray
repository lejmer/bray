trusted module memory.readiness;

trusted func main()
    uses(manual_alloc, raw_memory)
{
    let pointer: RawPointer<u8> = trusted core.memory.allocate(bytes = 8, align = 1);

    trusted core.memory.deallocate(pointer = pointer, bytes = 8, align = 1);
}
