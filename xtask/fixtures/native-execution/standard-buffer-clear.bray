module std.bytes;

func main() -> Result<unit, std.memory.MemoryLayoutError>
{
    let first: u8 = 1;
    let second: u8 = 2;
    let third: u8 = 3;
    let bytes: [u8; 3] = [first, second, third];
    let mut buffer: Buffer = try from_slice(&bytes[..]);

    clear(&mut buffer);

    assert(length(&buffer) == 0);

    return .Ok(unit);
}
