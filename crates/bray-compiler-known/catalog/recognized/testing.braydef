catalog recognized_standard_library revision 1;

scope StandardTesting at std.testing {
    declaration StandardTestingFail {
        identity name fail;
        implementation TestingFail;

        surface { func fail(pos message: string) -> never; }
    }
}
