module bray.standard_library_conformance.bytes;

using std.bytes;

@test
func byte_slice_operations()
{
    let first: u8 = 65;
    let second: u8 = 66;
    let bytes: [u8; 2] = [first, second];
    let length: usize = std.bytes.slice_length(&bytes[..]);
    let equal: bool = std.bytes.equals(&bytes[..], &bytes[..]);

    assert(length == 2);
    assert(equal);
}
