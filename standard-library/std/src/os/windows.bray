@target(target.identity.SYSTEM == "windows")
module std.os.windows;

@copy
@layout(transparent)
struct Handle
{
    internal raw: usize;

    func value() -> usize
    {
        return self.raw;
    }
}

func handle(pos raw: usize) -> Handle
{
    return
    {
        raw = raw,
    };
}
