@target(target.c.FLOAT == "r32")
module std.ffi.c;

@copy
@layout(transparent)
struct Float
{
    internal raw: r32;

    func value() -> r32
    {
        return self.raw;
    }
}

const func float_exact(pos raw: r32) -> Float
{
    return
    {
        raw = raw
    };
}
