@target(target.c.WCHAR == "u16")
module std.ffi.c;

@copy
@layout(transparent)
struct WideChar
{
    internal raw: u16;

    func value() -> u16
    {
        return self.raw;
    }
}

func wide_char(pos raw: u16) -> WideChar
{
    return
    {
        raw = raw
    };
}
