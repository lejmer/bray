@target(target.c.DOUBLE == "r64")
module std.ffi.c;

@copy
@layout(transparent)
struct Double
{
    internal raw: r64;

    func value() -> r64
    {
        return self.raw;
    }
}

const func double_exact(pos raw: r64) -> Double
{
    return
    {
        raw = raw
    };
}
