module std.run;

func cancellation_requested() -> bool
{
    return observe_cancellation();
}

func checkpoint()
{
    if observe_cancellation()
    {
        propagate_cancellation();
    }
}

extern internal func observe_cancellation() -> bool;

extern internal func propagate_cancellation();
